{
  "name": "canvas-chat",
  "version": "0.1.14",
  "description": "A visual, non-linear chat interface where conversations are nodes on an infinite canvas",
  "type": "module",
  "scripts": {
    "test": "node tests/test_utils.js && node tests/test_search.js && node tests/test_ui.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "private": true
}
